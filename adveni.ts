preapprove({
  projectId,
  location,
  featurestoreId,
  featureId,
  entityTypeSpecs,
  entityType,
  params,
  timeout
}): Promise<LROperation<protos.google.cloud.aiplatform.v1.PreapproveFeaturesResponse, protos.google.cloud.aiplatform.v1.PreapproveFeaturesOperationMetadata>>;
